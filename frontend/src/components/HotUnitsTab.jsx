// The hot-unit implementation remains in RankingPage.jsx as the extracted
// tab body, preserving its existing ranking, filtering, pagination, and
// recompute behavior. This component-level entry point keeps the tab reusable.
export { HotUnitsTab as default } from "../pages/RankingPage";
