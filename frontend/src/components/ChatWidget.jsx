import React, { useState } from "react";
import { useNavigate } from "react-router-dom";
export default function ChatWidget() { const [open, setOpen] = useState(false); const navigate = useNavigate(); return <div style={{position:"fixed",right:24,bottom:24,zIndex:60}}>{open && <div style={{width:300,padding:18,marginBottom:10,background:"#fff",border:"1px solid #ddd",borderRadius:16,boxShadow:"0 12px 40px #0002"}}><strong>AbsorpIQ Agent</strong><p style={{lineHeight:1.5,color:"#555"}}>Mở không gian chat đầy đủ để tiếp tục hội thoại và xem câu trả lời được định dạng.</p><button onClick={() => navigate("/ai-agent")} style={{width:"100%",padding:10,border:0,borderRadius:10,background:"#b8862d",color:"#fff"}}>Mở Agent</button></div>}<button type="button" onClick={() => setOpen((value) => !value)} style={{border:0,borderRadius:999,padding:"12px 18px",background:"#b8862d",color:"#fff",cursor:"pointer"}}>{open ? "Đóng Agent" : "Hỏi Agent"}</button></div>; }
